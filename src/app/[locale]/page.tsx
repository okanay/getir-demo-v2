'use client'
import { useEffect, useState } from 'react'
import { AnimatePresence, motion, MotionConfig, useAnimate } from 'framer-motion'
export default function Index({ params }: { params: { locale: string } }) {
   return <></>
}
